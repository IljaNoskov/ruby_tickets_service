<%= form_with(model: event) do |form| %>
  <% if event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(event.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
        <% event.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.number_field :name %>
  </div>

  <div class="field">
    <%= form.label :date %>
    <%= form.date_select :date %>
  </div>

  <div class="field">
    <%= form.label :ordinary_ticket_count %>
    <%= form.number_field :ordinary_ticket_count %>
  </div>

  <div class="field">
    <%= form.label :vip_ticket_count %>
    <%= form.number_field :vip_ticket_count %>
  </div>

  <div class="field">
    <%= form.label :ordinary_ticket_price %>
    <%= form.text_field :ordinary_ticket_price %>
  </div>

  <div class="field">
    <%= form.label :vip_ticket_count %>
    <%= form.text_field :vip_ticket_count %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
